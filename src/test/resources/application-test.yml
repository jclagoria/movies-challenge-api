movie:
  endpoint:
    url: 'https://eron-movies.wiremockapi.cloud/api/movies'

configuration:
  process:
    movies: 100

api:
  concurrency:
    limit: 10

cache:
  expiry:
    minutes: 1

logging:
  level:
    com:
      github:
        benmanes:
          caffeine: DEBUG